# ABZ Voice Server — Dependencies
# STT: Whisper (CPU int8) + TTS: Supertonic 3 (CPU)

# Voice Agent (LiveKit v1.0+)
livekit-agents[openai,silero]>=1.0.0
livekit-plugins-turn-detector>=0.1.0

# Audio Server
fastapi>=0.100.0
uvicorn>=0.20.0
python-multipart>=0.0.5
pydantic>=2.0.0

# STT
faster-whisper>=0.10.0

# TTS
supertonic>=3.0.0
numpy>=1.24.0

# Gateway HTTP client (used by agent.py → processar_texto)
aiohttp>=3.9.0
